"""Faithfulness-scaling pipeline package.

Modules:
    data         - loading and filtering the math problem eval set
    corruptions  - CoT corruption methods (truncate / substitute / remove)
    inference    - model backend abstraction (mock + hosted API stubs)
    scoring      - faithfulness rate computation and confidence intervals
"""
